# OpenAI Rate Limiting Configuration
# Copy these settings to your .env file and adjust based on your OpenAI tier

# Basic API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Rate Limits (adjust based on your OpenAI billing tier)
# Free tier: 3 RPM, 40,000 TPM
# Paid tier 1: 3,500 RPM, 200,000 TPM  
# Paid tier 5: 10,000 RPM, 2,000,000 TPM

# Requests per minute (default: 3500 for paid tier 1)
OPENAI_RPM_LIMIT=3500

# Tokens per minute (default: 200000 for paid tier 1)
OPENAI_TPM_LIMIT=200000

# Maximum concurrent requests (default: 100)
OPENAI_CONCURRENT_LIMIT=100

# Daily cost limit in USD (default: $50/day)
OPENAI_DAILY_COST_LIMIT=50.0

# Example configurations for different tiers:

# FREE TIER (very conservative)
# OPENAI_RPM_LIMIT=3
# OPENAI_TPM_LIMIT=40000
# OPENAI_CONCURRENT_LIMIT=1
# OPENAI_DAILY_COST_LIMIT=5.0

# PAID TIER 1 (default - good for most production use)
# OPENAI_RPM_LIMIT=3500
# OPENAI_TPM_LIMIT=200000
# OPENAI_CONCURRENT_LIMIT=100
# OPENAI_DAILY_COST_LIMIT=50.0

# PAID TIER 5 (high volume production)
# OPENAI_RPM_LIMIT=10000
# OPENAI_TPM_LIMIT=2000000
# OPENAI_CONCURRENT_LIMIT=500
# OPENAI_DAILY_COST_LIMIT=200.0