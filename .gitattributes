# Git LFS configuration for RareSift video assets - TEMPORARILY DISABLED for deployment
# Track all MP4 video files with LFS for efficient storage
# Video files - use LFS for large binary assets - COMMENTED OUT FOR LFS BUDGET WORKAROUND
# *.MP4 filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# Other large binary files that might be added later - COMMENTED OUT FOR LFS BUDGET WORKAROUND
# *.mov filter=lfs diff=lfs merge=lfs -text
# *.avi filter=lfs diff=lfs merge=lfs -text
# *.mkv filter=lfs diff=lfs merge=lfs -text
# Large image files if needed - COMMENTED OUT FOR LFS BUDGET WORKAROUND
# *.tiff filter=lfs diff=lfs merge=lfs -text
# *.psd filter=lfs diff=lfs merge=lfs -text
# Archive files - COMMENTED OUT FOR LFS BUDGET WORKAROUND
# *.zip filter=lfs diff=lfs merge=lfs -text
# *.tar.gz filter=lfs diff=lfs merge=lfs -text
