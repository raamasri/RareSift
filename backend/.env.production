# RareSift Backend Production Environment Configuration
# 
# IMPORTANT: This file contains template values and references to secrets.
# In production, sensitive values should be managed through:
# - Render Environment Variables dashboard
# - HashiCorp Vault
# - AWS Secrets Manager
# - Other secure secrets management systems

# Application Environment
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# Database Configuration (set in Render dashboard)
DATABASE_URL=${DATABASE_URL}
REDIS_URL=${REDIS_URL}

# Security Configuration (set in Render dashboard)
SECRET_KEY=${SECRET_KEY}
JWT_SECRET=${JWT_SECRET}
CORS_ORIGINS=${CORS_ORIGINS:-https://your-frontend-domain.vercel.app}

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=RareSift
VERSION=1.0.0

# File Upload Configuration
MAX_UPLOAD_SIZE=500000000  # 500MB
UPLOAD_DIR=/tmp/uploads
ALLOWED_EXTENSIONS=mp4,mov,avi,mkv

# AI/ML Configuration
CLIP_MODEL_NAME=ViT-B/32
EMBEDDING_DIM=512
BATCH_SIZE=10
MAX_WORKERS=4

# Performance Configuration
CACHE_TTL=3600
REDIS_TTL=1800
DATABASE_POOL_SIZE=20
DATABASE_POOL_OVERFLOW=30

# External Services (set in Render dashboard)
OPENAI_API_KEY=${OPENAI_API_KEY}
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
AWS_REGION=${AWS_REGION:-us-east-1}
S3_BUCKET=${S3_BUCKET}

# Monitoring Configuration
ENABLE_METRICS=true
METRICS_PORT=9090
HEALTH_CHECK_TIMEOUT=30

# Render-specific Configuration
PORT=${PORT:-8000}
HOST=0.0.0.0